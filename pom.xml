<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>grupo6</groupId>
    <artifactId>trabalho-intro-dev-web</artifactId>
    <version>2.0.0-SNAPSHOT</version>
    <packaging>war</packaging>
    <name>trabalho-intro-dev-web</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jakartaee>10.0.0</jakartaee> <!-- Versão da API Jakarta EE utilizada no projeto -->
    </properties>

    <dependencies>
        <!-- Dependência da API Jakarta EE para desenvolvimento de aplicações web (Servlets, JSP, etc) -->
        <dependency>
            <groupId>jakarta.platform</groupId>
            <artifactId>jakarta.jakartaee-api</artifactId>
            <version>${jakartaee}</version>
            <!-- O servidor de aplicação (GlassFish) fornecerá essa API em tempo de execução -->
            <scope>provided</scope> 
        </dependency>

        <!-- Dependência do driver JDBC para o MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>8.0.33</version>
        </dependency>

        <!-- JSTL Jakarta EE 10 (API fornecida pelo servidor + implementação) -->
        <dependency>
            <groupId>jakarta.servlet.jsp.jstl</groupId>
            <artifactId>jakarta.servlet.jsp.jstl-api</artifactId>
            <version>3.0.0</version>
            <scope>provided</scope> <!-- API fornecida pelo GlassFish -->
        </dependency>

        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>jakarta.servlet.jsp.jstl</artifactId>
            <version>3.0.0</version> <!-- Implementação da JSTL necessária para GlassFish -->
        </dependency>
        
        <!-- Dependência do JUnit 5, para escrever e rodar testes unitários em Java -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>5.10.0</version>
            <scope>test</scope>
        </dependency>
        
<<<<<<< HEAD
        <dependency>
		    <groupId>org.junit.jupiter</groupId>
		    <artifactId>junit-jupiter-engine</artifactId>
		    <version>5.10.0</version>
		    <scope>test</scope>
		</dependency>
=======
        <!-- Engine do JUnit 5 (Adicionada para garantir a execução correta via Maven/IDE) -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>5.10.0</version>
            <scope>test</scope>
        </dependency>
>>>>>>> cb50c3651d20466f989b64b8e24254f5a7e9b278

		<!-- Dependência do Mockito, para a criação de 'mocks' nos testes unitários. -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>5.5.0</version>
            <scope>test</scope>
        </dependency>
<<<<<<< HEAD
        
        <!-- Dependência do Selenium, para testes automatizados -->
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>4.18.1</version> <!-- Versão estável recente -->
        </dependency>
   
=======

        <!-- =================================================================== -->
        <!-- NOVAS DEPENDÊNCIAS PARA TESTES DE SISTEMA (SELENIUM) -->
        <!-- =================================================================== -->

        <!-- Selenium Java: A biblioteca principal para automação do navegador -->
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>4.16.1</version>
            <scope>test</scope>
        </dependency>

        <!-- WebDriverManager: Gerencia automaticamente o download do driver do Chrome/Firefox -->
        <dependency>
            <groupId>io.github.bonigarcia</groupId>
            <artifactId>webdrivermanager</artifactId>
            <version>5.6.3</version>
            <scope>test</scope>
        </dependency>

>>>>>>> cb50c3651d20466f989b64b8e24254f5a7e9b278
    </dependencies>

    <build>
        <plugins>
            <!-- Plugin para compilação do código Java. Configura a versão para o JDK 11. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>11</source>
                    <target>11</target>
                </configuration>
            </plugin>

            <!-- Plugin para empacotar a aplicação como um arquivo WAR. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.3.2</version>
            </plugin>
            
            <plugin>
			    <groupId>org.apache.maven.plugins</groupId>
			    <artifactId>maven-surefire-plugin</artifactId>
			    <version>3.2.5</version> 
			    <configuration>
			        <includes>
			            <include>**/*Test.java</include>
			        </includes>
			    </configuration>	
			</plugin>
                        
            <plugin>
			    <groupId>org.pitest</groupId>
			    <artifactId>pitest-maven</artifactId>
			    <version>1.15.0</version>
			
			    <configuration>
			        <targetClasses>
			            <param>br.uff.ic.grupo6.banco.service.*</param>
			        </targetClasses>
			
			        <targetTests>
			            <param>br.uff.ic.grupo6.banco.estrutural.*</param>
			        </targetTests>
			
			        <excludedClasses>
			            <param>br.uff.ic.grupo6.banco.controller.*</param>
			            <param>br.uff.ic.grupo6.banco.dao.*</param>
			            <param>br.uff.ic.grupo6.banco.model.*</param>
			        </excludedClasses>
			        
			        <mutators>
			            <mutator>STRONGER</mutator>
			        </mutators>
			
			        <reportsDirectory>${project.build.directory}/pit-reports</reportsDirectory>
			        <verbose>true</verbose>
			    </configuration>
			
			    <dependencies>
			        <dependency>
			            <groupId>org.pitest</groupId>
			            <artifactId>pitest-junit5-plugin</artifactId>
			            <version>1.2.1</version>
			        </dependency>
			        
			        <dependency>
			            <groupId>org.junit.jupiter</groupId>
			            <artifactId>junit-jupiter-engine</artifactId>
			            <version>5.10.0</version>
			        </dependency>
			
			        <dependency>
			            <groupId>org.junit.platform</groupId>
			            <artifactId>junit-platform-launcher</artifactId>
			            <version>1.10.0</version>
			        </dependency>
			    </dependencies>
			</plugin>
                   
        </plugins>
    </build>
</project>